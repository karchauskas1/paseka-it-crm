# 🚀 Импорт проекта в Vercel - Пошаговая инструкция

## Проблема
Проект создан в Vercel, переменные окружения добавлены, но деплои не создаются автоматически при push в GitHub.

## Решение
Нужно правильно импортировать проект из GitHub репозитория.

---

## 📋 Шаги:

### 1. Удалите текущий проект (если он пустой)

Если в Vercel уже есть проект `paseka-it-crm` без деплоев:

1. Откройте проект в Vercel
2. **Settings** → **Advanced** → **Delete Project**
3. Подтвердите удаление

**ИЛИ** просто создайте новый проект с другим именем (например, `paseka-it-crm-production`)

---

### 2. Импортируйте проект из GitHub

#### Вариант A: Быстрая ссылка (рекомендуется)

Откройте эту ссылку:
```
https://vercel.com/new/clone?repository-url=https://github.com/karchauskas1/paseka-it-crm
```

#### Вариант B: Вручную

1. Откройте https://vercel.com/new
2. Выберите **Import Git Repository**
3. Найдите `karchauskas1/paseka-it-crm` в списке
4. Нажмите **Import**

---

### 3. Настройка проекта

На экране конфигурации:

**Project Name:** `paseka-it-crm` (или любое другое)

**Framework Preset:** `Next.js` (автоматически определится)

**Root Directory:** `.` (оставьте по умолчанию)

**Build Command:** (оставьте пустым - будет использовано из package.json)

**Output Directory:** `.next` (автоматически)

---

### 4. Environment Variables

**ВАЖНО:** Добавьте переменные ДО первого деплоя!

Нажмите **Environment Variables** и добавьте:

#### DATABASE_URL
```
Вставьте значение из Neon (или создайте новую БД через Storage)
```
**Scope:** ✅ Production, ✅ Preview, ✅ Development

#### JWT_SECRET
```
dK8mX9pL2vN4qR7sT1wY6zA3bC5eF8hJ0kM2nP4rU9x
```
**Scope:** ✅ Production, ✅ Preview, ✅ Development

#### OPENROUTER_API_KEY
```
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
```
**Scope:** ✅ Production, ✅ Preview, ✅ Development

#### TELEGRAM_BOT_TOKEN
```
8521292562:AAE_AHcln44V_K7SVHoxXBNPx1k1y-ZJifM
```
**Scope:** ✅ Production, ✅ Preview, ✅ Development

---

### 5. Deploy

Нажмите **Deploy** и подождите 2-3 минуты!

---

### 6. Подключите базу данных Neon

После первого деплоя (даже если он упал):

1. Перейдите в **Storage** → **Connect Store**
2. Выберите **Neon** → вашу существующую БД `PASEKA_IT_CRM`
3. **Connect**
4. Это автоматически добавит все DATABASE переменные

---

### 7. Применить Prisma схему

После успешного деплоя выполните:

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите
vercel login

# Перейдите в проект
cd "/Users/karchauskas/-TG bots/PASEKA IT CRM"

# Подключитесь к Vercel проекту
vercel link

# Примените схему БД
vercel --prod exec -- npx prisma db push
```

---

## ✅ Проверка

После всех шагов:

1. Откройте **Deployments** в Vercel - должен быть хотя бы один деплой
2. Статус деплоя: **Ready** (зеленая галочка)
3. Кликните на URL деплоя - приложение должно открыться
4. Страница логина загружается
5. Можно зарегистрироваться

---

## 🔄 Автоматические деплои

После импорта проекта из GitHub:

- ✅ Каждый `git push` в `main` → Production деплой
- ✅ Каждый push в другую ветку → Preview деплой
- ✅ Каждый Pull Request → Preview деплой с комментарием

---

## 🐛 Troubleshooting

### Проект не находится в списке репозиториев

1. Vercel → **Account Settings** → **Git**
2. Проверьте что GitHub подключен
3. **Configure Git Integration** → дайте доступ к репозиторию

### База данных не подключается

- Используйте `DATABASE_URL` (без `_POOLED` или `_PRISMA`)
- Формат: `postgresql://user:pass@host:5432/db`

### Prisma генерация падает

Проверьте что в `package.json` есть:
```json
{
  "scripts": {
    "postinstall": "prisma generate"
  }
}
```

---

**🎯 Следуйте этой инструкции и проект корректно задеплоится!**
