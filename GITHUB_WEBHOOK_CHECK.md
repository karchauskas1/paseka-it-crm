# 🔍 Проверка GitHub Webhook для Vercel

## Проблема

Git подключен к Vercel, но деплои не создаются автоматически при push.

## Решение

Проверить и настроить webhook на GitHub.

---

## 📋 Шаги проверки:

### 1. Откройте настройки репозитория на GitHub

https://github.com/karchauskas1/paseka-it-crm/settings/hooks

### 2. Проверьте наличие webhook от Vercel

Должен быть webhook с URL типа:
```
https://api.vercel.com/v1/integrations/webhooks/...
```

### 3. Проверьте статус webhook

**Зеленая галочка ✅** - webhook работает корректно

**Красный крестик ❌** или **желтый предупреждающий знак** - webhook не работает

### 4. Посмотрите Recent Deliveries

Кликните на webhook → **Recent Deliveries**

Должны быть записи о последних push событиях.

**Если есть ошибки (красные):**
- Кликните на запись
- Посмотрите Response (ответ от Vercel)
- Это покажет причину проблемы

---

## 🔧 Если webhook отсутствует:

### Вариант A: Создать webhook вручную

На GitHub:

1. **Settings** → **Webhooks** → **Add webhook**
2. **Payload URL**: (нужно получить из Vercel)
3. **Content type**: `application/json`
4. **Events**: `Just the push event`
5. **Active**: ✅
6. **Add webhook**

**Где взять Payload URL:**
- Vercel → Settings → Git → должен показывать webhook URL
- Или создайте Deploy Hook в Vercel и используйте его

### Вариант B: Переустановить Vercel GitHub App

1. Откройте https://github.com/settings/installations
2. Найдите **Vercel**
3. **Configure**
4. Убедитесь что `paseka-it-crm` репозиторий имеет доступ
5. Если нет - добавьте его в **Repository access**

---

## 🎯 Альтернативное решение: Deploy Hook

Если webhook не работает, можно использовать Deploy Hook:

### Создать Deploy Hook в Vercel:

1. Vercel → **Settings** → **Git** → **Deploy Hooks**
2. **Name**: `GitHub main`
3. **Branch**: `main`
4. **Create Hook**
5. Скопируйте URL

### Добавить webhook на GitHub:

1. GitHub → **Settings** → **Webhooks** → **Add webhook**
2. **Payload URL**: вставьте URL из Deploy Hook
3. **Content type**: `application/json`
4. **Events**: `Just the push event`
5. **Active**: ✅
6. **Add webhook**

---

## ✅ Проверка работы

После настройки webhook:

1. Сделайте push в main:
   ```bash
   git commit --allow-empty -m "Test webhook"
   git push origin main
   ```

2. На GitHub → Settings → Webhooks → ваш webhook → Recent Deliveries
   - Должна появиться новая запись
   - Статус: 200 OK (зеленая галочка)

3. В Vercel → Deployments
   - Должен появиться новый деплой

---

## 🐛 Частые проблемы:

### Webhook есть, но показывает ошибки

**Причина**: Неправильный URL или Vercel не может обработать запрос

**Решение**:
- Удалите webhook
- Переподключите Git в Vercel (Disconnect → Connect)
- Webhook пересоздастся автоматически

### Webhook срабатывает, но деплоя нет

**Причина**: Vercel проект не связан с репозиторием

**Решение**:
- Vercel → Settings → Git → должен показывать `karchauskas1/paseka-it-crm`
- Если нет - Disconnect и Connect заново

### Recent Deliveries пустой

**Причина**: Webhook еще не получал события

**Решение**: Сделайте тестовый push

---

## 📊 Следующие шаги:

После того как webhook заработает:

1. ✅ Каждый push → автоматический деплой
2. ✅ GitHub checks будут проходить
3. ✅ Можно будет использовать Pull Requests с Preview деплоями

---

**Проверьте webhook на GitHub и напишите что там!** 🔍
