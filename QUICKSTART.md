# Быстрый старт PASEKA IT CRM

Этот файл содержит пошаговую инструкцию для локального запуска CRM за 5 минут.

## Шаг 1: Установите зависимости

```bash
npm install
```

Если используете Bun (быстрее):
```bash
bun install
```

## Шаг 2: Настройте PostgreSQL

### Вариант А: Использовать локальный PostgreSQL

Установите PostgreSQL если ещё не установлен:
- **macOS**: `brew install postgresql@16 && brew services start postgresql@16`
- **Windows**: Скачайте с https://www.postgresql.org/download/
- **Linux**: `sudo apt-get install postgresql`

Создайте базу данных:
```bash
# Войдите в psql
psql postgres

# Создайте базу данных
CREATE DATABASE paseka_crm;

# Создайте пользователя (опционально)
CREATE USER paseka_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE paseka_crm TO paseka_user;

# Выйдите
\q
```

### Вариант Б: Использовать облачный PostgreSQL (Neon/Supabase)

1. Зарегистрируйтесь на [Neon](https://neon.tech) или [Supabase](https://supabase.com)
2. Создайте новый проект
3. Скопируйте connection string

## Шаг 3: Создайте .env файл

Скопируйте пример:
```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Обязательные поля:
DATABASE_URL="postgresql://user:password@localhost:5432/paseka_crm"
JWT_SECRET="ваш_секретный_ключ_минимум_32_символа"

# Опциональные (можно заполнить позже):
OPENROUTER_API_KEY="your-key-here"
TELEGRAM_BOT_TOKEN="your-token-here"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

Для генерации JWT_SECRET используйте:
```bash
openssl rand -base64 32
```

Или просто используйте любую длинную строку (минимум 32 символа).

## Шаг 4: Настройте базу данных

```bash
# Примените схему к базе данных
npm run db:push

# Сгенерируйте Prisma Client
npm run db:generate
```

## Шаг 5: Запустите приложение

```bash
npm run dev
```

Откройте браузер: http://localhost:3000

## Шаг 6: Первый вход

1. Нажмите "Зарегистрироваться"
2. Заполните форму:
   - Имя: Ваше имя
   - Email: ваш@email.com
   - Пароль: минимум 6 символов

3. Вы автоматически войдёте в систему

## Готово! 🎉

Теперь вы можете:
- Создавать клиентов в разделе "Клиенты"
- Создавать проекты в разделе "Проекты"
- Добавлять задачи
- И многое другое!

---

## Опционально: Настройка AI и Telegram

### Настройка AI (OpenRouter)

1. Зарегистрируйтесь на https://openrouter.ai/
2. Пополните баланс ($5-10 хватит надолго)
3. Создайте API ключ в Settings → API Keys
4. Добавьте в `.env`:
   ```
   OPENROUTER_API_KEY="sk-or-v1-..."
   ```
5. Перезапустите сервер

Теперь AI будет помогать:
- Анализировать боль клиента
- Предлагать архитектуру решения
- Автодополнение полей

### Настройка Telegram уведомлений

1. Найдите в Telegram: @BotFather
2. Отправьте команду: `/newbot`
3. Следуйте инструкциям, дайте боту имя
4. Скопируйте токен (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Добавьте в `.env`:
   ```
   TELEGRAM_BOT_TOKEN="1234567890:ABC..."
   ```
6. Перезапустите сервер

Чтобы получать уведомления:
1. Найдите вашего бота в Telegram по имени
2. Отправьте `/start`
3. Бот пришлёт вам ID (например: `123456789`)
4. Зайдите в настройки профиля в CRM
5. Введите этот ID в поле "Telegram ID"
6. Сохраните

Теперь вы будете получать уведомления о:
- Изменениях статусов проектов
- Новых задачах
- Комментариях
- Приближающихся дедлайнах

---

## Troubleshooting

### Ошибка подключения к БД

Проверьте:
- PostgreSQL запущен: `pg_isready`
- DATABASE_URL правильный в `.env`
- База данных создана: `psql -l | grep paseka_crm`

### Prisma ошибка

Попробуйте:
```bash
npx prisma generate
npx prisma db push --force-reset
```

### Порт 3000 занят

Измените порт:
```bash
PORT=3001 npm run dev
```

### Ошибки TypeScript

```bash
rm -rf .next node_modules
npm install
npm run dev
```

---

## Что дальше?

1. Прочитайте полную документацию в [README.md](README.md)
2. Создайте первого клиента
3. Создайте первый проект
4. Пригласите коллег в workspace
5. Настройте AI и Telegram для полного функционала

**Нужна помощь?** Проверьте логи в консоли или откройте Issue на GitHub.

---

Приятного использования! 🚀
