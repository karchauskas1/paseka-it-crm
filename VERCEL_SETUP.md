# 🚀 Деплой PASEKA IT CRM на Vercel

## Шаг 1: Подготовка базы данных

### Вариант A: Vercel Postgres (Рекомендуется)

1. Откройте https://vercel.com/new
2. Перейдите в **Storage** → **Create Database**
3. Выберите **Postgres**
4. Выберите регион (Europe - Frankfurt для лучшей скорости в РФ)
5. Нажмите **Create**
6. Скопируйте `DATABASE_URL` из `.env.local` таб

### Вариант B: Supabase (Бесплатно)

1. Откройте https://supabase.com
2. Создайте новый проект
3. Project Settings → Database → Connection String → URI
4. Скопируйте строку подключения

### Вариант C: Railway (Бесплатно)

1. Откройте https://railway.app
2. New Project → Provision PostgreSQL
3. PostgreSQL → Connect → Postgres Connection URL
4. Скопируйте URL

## Шаг 2: Импорт проекта в Vercel

1. Откройте https://vercel.com/new
2. Нажмите **Import Git Repository**
3. Выберите репозиторий: `karchauskas1/paseka-it-crm`
4. Нажмите **Import**

## Шаг 3: Настройка Environment Variables

В разделе **Environment Variables** добавьте следующие переменные:

### Обязательные переменные:

```bash
DATABASE_URL
```
Значение: Строка подключения из Шага 1 (например: `postgresql://user:password@host:5432/database`)

```bash
JWT_SECRET
```
Значение: Случайная строка (32+ символов)
Генерация: `openssl rand -base64 32`

### Опциональные переменные:

```bash
OPENROUTER_API_KEY
```
Значение: `sk-or-v1-...` (для AI функций)
Получить: https://openrouter.ai/keys

```bash
TELEGRAM_BOT_TOKEN
```
Значение: Токен бота (для уведомлений)
Получить: https://t.me/BotFather

```bash
NEXT_PUBLIC_APP_URL
```
Значение: `https://your-app.vercel.app` (будет показано после деплоя)

## Шаг 4: Deploy

1. Нажмите **Deploy**
2. Подождите 2-3 минуты
3. После успешного деплоя скопируйте URL проекта

## Шаг 5: Инициализация базы данных

После первого деплоя нужно применить Prisma схему:

### Вариант A: Через Vercel CLI (Рекомендуется)

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Перейдите в директорию проекта
cd "/Users/karchauskas/-TG bots/PASEKA IT CRM"

# Подключитесь к проекту
vercel link

# Выполните команду в production
vercel env pull .env.production
npx prisma db push
```

### Вариант B: Через Vercel Dashboard

1. Откройте ваш проект в Vercel
2. Settings → Functions → Environment Variables
3. Временно добавьте переменную:
   ```
   PRISMA_GENERATE_DATAPROXY=true
   ```
4. Deployments → последний деплой → ... → Redeploy
5. После деплоя удалите эту переменную

### Вариант C: Через удаленную консоль БД

Если у вас Supabase или другой провайдер с SQL редактором:

1. Откройте SQL редактор вашей БД
2. Скопируйте SQL схему из Prisma:
   ```bash
   npx prisma migrate dev --name init --create-only
   ```
3. Выполните SQL в редакторе

## Шаг 6: Обновление NEXT_PUBLIC_APP_URL

1. Скопируйте URL вашего деплоя (например: `https://paseka-it-crm.vercel.app`)
2. Vercel Dashboard → Settings → Environment Variables
3. Найдите `NEXT_PUBLIC_APP_URL` (или создайте новую)
4. Установите значение: ваш URL
5. Deployments → последний деплой → ... → Redeploy

## Шаг 7: Создание первого пользователя

Откройте ваше приложение и зарегистрируйтесь:

1. Откройте `https://your-app.vercel.app`
2. Нажмите "Регистрация"
3. Создайте первого пользователя (автоматически получит роль OWNER)

## 🎉 Готово!

Ваше приложение развернуто и готово к использованию!

## 📊 Проверка работоспособности

- [ ] Приложение открывается по URL
- [ ] Страница регистрации работает
- [ ] Можно создать пользователя
- [ ] Можно войти в систему
- [ ] Dashboard отображается
- [ ] Можно создать проект

## 🔄 Автоматические деплои

Теперь каждый `git push` в `main` ветку автоматически запустит новый деплой на Vercel!

```bash
# Внесите изменения
git add .
git commit -m "Update feature"
git push origin main

# Vercel автоматически задеплоит изменения
```

## ⚙️ Настройка домена (опционально)

См. инструкцию выше в предыдущем ответе.

## 🐛 Troubleshooting

### Ошибка: "Prisma Client did not initialize"

**Решение**: Добавьте `postinstall` скрипт в `package.json`:
```json
{
  "scripts": {
    "postinstall": "prisma generate"
  }
}
```

### Ошибка: "DATABASE_URL environment variable is not set"

**Решение**: Проверьте что `DATABASE_URL` добавлена в Environment Variables и redeploy проект.

### Ошибка: "connect ECONNREFUSED"

**Решение**: Проверьте правильность DATABASE_URL. Убедитесь что БД доступна из интернета.

### Ошибка 500 на странице

**Решение**: Откройте Vercel → Functions → Logs и посмотрите детали ошибки.

## 📞 Поддержка

- Vercel Docs: https://vercel.com/docs
- Prisma Docs: https://www.prisma.io/docs
- Next.js Docs: https://nextjs.org/docs

---

**Создано с Claude Sonnet 4.5** 🤖
