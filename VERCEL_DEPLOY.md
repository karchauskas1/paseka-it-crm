# Деплой на Vercel - Пошаговая инструкция

## 1. Подготовка базы данных

Вам нужна PostgreSQL база данных. Варианты:

### Вариант A: Vercel Postgres (рекомендуется)
1. Перейдите на https://vercel.com/dashboard
2. Откройте ваш проект
3. Перейдите в Storage → Create Database → Postgres
4. Vercel автоматически создаст и подключит базу данных

### Вариант B: Supabase (бесплатно)
1. Создайте проект на https://supabase.com
2. Скопируйте Connection String из Settings → Database
3. Используйте формат: `postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres`

### Вариант C: Railway (бесплатно)
1. Создайте PostgreSQL на https://railway.app
2. Скопируйте DATABASE_URL из переменных окружения

## 2. Автоматический деплой через GitHub

### Шаг 1: Подключите репозиторий к Vercel

1. Перейдите на https://vercel.com/new
2. Выберите "Import Git Repository"
3. Найдите `karchauskas1/paseka-it-crm`
4. Нажмите Import

### Шаг 2: Настройте переменные окружения

В настройках проекта добавьте:

```bash
# Обязательные
DATABASE_URL=postgresql://user:password@host:5432/dbname
JWT_SECRET=generate-random-string-32-chars

# Опциональные (для AI функций)
OPENROUTER_API_KEY=sk-or-v1-your-key

# Опциональные (для Telegram уведомлений)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID=-1001234567890

# Public URL (Vercel установит автоматически)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
```

### Шаг 3: Деплой!

1. Нажмите "Deploy"
2. Дождитесь завершения сборки (2-3 минуты)
3. После деплоя выполните миграцию БД:

```bash
# Установите Vercel CLI
npm i -g vercel

# Залогиньтесь
vercel login

# Подключитесь к проекту
vercel link

# Загрузите переменные окружения
vercel env pull .env

# Выполните миграцию
npx prisma db push
```

## 3. Автоматические деплои при push в GitHub

После первоначальной настройки:

1. Любой push в ветку `main` автоматически запускает деплой
2. Pull requests создают preview деплои
3. Vercel автоматически запускает `prisma generate` при сборке

## 4. Первый запуск

После успешного деплоя:

1. Откройте ваше приложение (https://your-app.vercel.app)
2. Перейдите на `/register` (если нет пользователей)
3. Зарегистрируйте первого пользователя
4. Создайте workspace
5. Готово! 🎉

## 5. Мониторинг и логи

- **Логи**: https://vercel.com/[username]/[project]/logs
- **Аналитика**: https://vercel.com/[username]/[project]/analytics
- **Performance**: Vercel автоматически мониторит производительность

## 6. Обновление приложения

Просто сделайте push в GitHub:

```bash
git add .
git commit -m "Your update"
git push origin main
```

Vercel автоматически задеплоит изменения!

## Проблемы?

### База данных не подключается
- Проверьте формат DATABASE_URL
- Убедитесь, что база доступна извне
- Для Vercel Postgres проверьте регион (должен совпадать с проектом)

### Ошибка при сборке "Prisma"
- Vercel автоматически выполняет `prisma generate`
- Если ошибка - проверьте `prisma/schema.prisma`

### Не работают переменные окружения
- Убедитесь, что добавили их в Vercel Dashboard
- Пересоберите проект после добавления переменных

## Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Docs - Next.js](https://vercel.com/docs/frameworks/nextjs)
- [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres)
- [Документация Prisma](https://www.prisma.io/docs)

---

**Готово!** Теперь ваше приложение автоматически деплоится при каждом push в GitHub! 🚀
