# 🚀 Инструкции по деплою на production

## ✅ Что уже сделано

1. **Код отправлен на GitHub:**
   - Коммит: `b610c41`
   - Branch: `main`
   - Репозиторий обновлён

2. **Изменения в коде:**
   - Добавлены кастомные поля (CustomFields)
   - Реализован Telegram бот для уведомлений
   - Добавлено поле website для клиентов
   - Исправлены баги и добавлена документация

## 📋 Что нужно сделать на production

### Шаг 1: Применить миграции базы данных

**Важно:** Необходимо применить 2 новые миграции:
1. `20260119164550_add_website_to_clients` - добавляет поле `website` в таблицу `clients`
2. `20260119165609_add_telegram_name_to_users` - добавляет поле `telegram_name` в таблицу `users`

#### Вариант А: Автоматически через Vercel (рекомендуется)

Если проект на Vercel, миграции применятся автоматически при деплое.

Проверьте логи деплоя на наличие:
```
✓ Running migrations...
✓ Applied 2 migrations
```

#### Вариант Б: Вручную через Vercel CLI

```bash
# 1. Установите Vercel CLI (если ещё не установлен)
npm i -g vercel

# 2. Войдите в аккаунт
vercel login

# 3. Подключитесь к проекту
vercel link

# 4. Скачайте переменные окружения
vercel env pull .env.production

# 5. Примените миграции
npx prisma migrate deploy
```

#### Вариант В: Вручную через SQL (если нет доступа к Vercel CLI)

Подключитесь к production БД через Neon Dashboard или pgAdmin и выполните:

```sql
-- Миграция 1: Добавить поле website
ALTER TABLE clients ADD COLUMN IF NOT EXISTS website TEXT;

-- Миграция 2: Добавить поле telegram_name
ALTER TABLE users ADD COLUMN IF NOT EXISTS telegram_name TEXT;

-- Проверка
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'clients' AND column_name = 'website';

SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'users' AND column_name = 'telegram_name';
```

### Шаг 2: Настроить переменные окружения (если требуется)

Если вы планируете использовать Telegram бот, добавьте в Vercel:

```
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather
```

**Как получить токен:**
1. Откройте Telegram и найдите @BotFather
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен
4. Добавьте его в переменные окружения Vercel

### Шаг 3: Настроить Telegram webhook (опционально)

После деплоя, если хотите использовать Telegram бота:

```bash
# Установите webhook через API Telegram
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.vercel.app/api/telegram/webhook"}'
```

Или через админ панель:
1. Перейдите в Администрирование → Интеграции → Telegram
2. Введите Bot Token
3. Нажмите "Сохранить"

### Шаг 4: Проверка деплоя

После деплоя проверьте:

#### ✅ Основной функционал
- [ ] Страница клиентов открывается без ошибок
- [ ] Создание клиента работает
- [ ] Поле "Вебсайт" отображается в форме

#### ✅ Кастомные поля
- [ ] Администрирование → Кастомные поля открывается
- [ ] Можно создать новое кастомное поле
- [ ] Кастомное поле появляется в форме клиента

#### ✅ Telegram бот (если настроен)
- [ ] Отправьте `/start` боту в Telegram
- [ ] Бот отвечает с вашим Telegram ID
- [ ] В админ-панели "Тест" отправляет сообщение

#### ✅ Другие фичи
- [ ] Колокольчик уведомлений работает
- [ ] Кнопка "Обратная связь" видна
- [ ] Задачи открываются из календаря

## 🐛 Troubleshooting

### Ошибка: "Column 'website' does not exist"

**Решение:** Миграции не применились. Примените их вручную (см. Шаг 1, Вариант В)

### Ошибка: "Cannot find module '@/components/custom-fields/...'"

**Решение:** Vercel не пересобрал проект. Форсируйте redeploy:
```bash
vercel --prod --force
```

### Telegram бот не отвечает

**Проверьте:**
1. Токен бота корректен в переменных окружения
2. Webhook установлен правильно: `https://api.telegram.org/bot<TOKEN>/getWebhookInfo`
3. В логах Vercel нет ошибок при вызове `/api/telegram/webhook`

### База данных показывает старые данные

**Решение:** Очистите кэш:
```bash
# В Vercel
vercel env pull .env.production
npx prisma generate
```

## 📊 Мониторинг

После деплоя отслеживайте:

1. **Логи Vercel:**
   ```bash
   vercel logs --prod
   ```

2. **Ошибки в БД:**
   - Проверьте Neon Dashboard → Monitoring

3. **Telegram webhook:**
   ```bash
   curl https://api.telegram.org/bot<TOKEN>/getWebhookInfo
   ```

## 📝 Changelog

### Версия: b610c41 (19.01.2026)

**Новые фичи:**
- ✨ Система кастомных полей с полным UI
- ✨ Telegram бот для уведомлений (команды /start, /help, /status)
- ✨ Поле website для клиентов с валидацией
- ✨ Документация AI функционала
- ✨ Кнопка "Обратная связь" в навигации

**Исправления:**
- 🐛 Select.Item пустые значения в диалоге задач
- 🐛 Открытие задач из календаря
- 🐛 Production fallback для socialLinks/customFields

**Миграции:**
- `20260119164550_add_website_to_clients`
- `20260119165609_add_telegram_name_to_users`

---

## 🎯 Быстрый чеклист деплоя

```
[ ] 1. Код отправлен на GitHub (git push)
[ ] 2. Vercel автоматически задеплоил (проверить Dashboard)
[ ] 3. Миграции применены (проверить логи)
[ ] 4. Страница клиентов работает
[ ] 5. Админ панель → Кастомные поля доступна
[ ] 6. (Опционально) Telegram бот настроен и отвечает
```

**Готово!** 🚀

Если возникли проблемы, проверьте секцию Troubleshooting выше.
