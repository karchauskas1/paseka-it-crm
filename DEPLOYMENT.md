# 🚀 Деплой готов!

## Что уже настроено:

✅ **GitHub репозиторий**: https://github.com/karchauskas1/paseka-it-crm
✅ **Код залит** на GitHub
✅ **Vercel конфигурация** готова (`vercel.json`)
✅ **Auto-deploy** настроен (при push в main)
✅ **Prisma generate** автоматически выполняется при сборке

## Следующие шаги для автоматического деплоя:

### 1. Подключите репозиторий к Vercel

**Вариант A: Через веб-интерфейс (проще)**

1. Откройте: https://vercel.com/new
2. Войдите через GitHub
3. Выберите репозиторий `karchauskas1/paseka-it-crm`
4. Нажмите **Import**

**Вариант B: Через кнопку Deploy**

Просто нажмите на кнопку в README.md на GitHub!

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/karchauskas1/paseka-it-crm)

### 2. Добавьте переменные окружения в Vercel

В настройках проекта на Vercel добавьте:

#### Обязательные:
```
DATABASE_URL=postgresql://user:password@host:5432/paseka_crm
JWT_SECRET=your-super-secret-32-character-key
```

#### Опциональные (для AI):
```
OPENROUTER_API_KEY=sk-or-v1-your-key
```

#### Опциональные (для Telegram):
```
TELEGRAM_BOT_TOKEN=123456:ABC-DEF
TELEGRAM_CHAT_ID=-1001234567890
```

### 3. Где взять базу данных PostgreSQL?

**Вариант 1: Vercel Postgres** (рекомендуется, $0-20/мес)
- Storage → Create Database → Postgres
- Автоматически подключится к проекту

**Вариант 2: Supabase** (бесплатно, 500MB)
- https://supabase.com → New Project
- Settings → Database → Connection String

**Вариант 3: Railway** (бесплатно, $5 кредит)
- https://railway.app → New Project → PostgreSQL
- Variables → DATABASE_URL

### 4. Первый деплой

1. Нажмите **Deploy** на Vercel
2. Дождитесь сборки (~2-3 минуты)
3. После деплоя **выполните миграцию БД**:

```bash
# Установите Vercel CLI
npm i -g vercel

# Подключитесь
vercel link

# Загрузите env
vercel env pull .env

# Выполните миграцию
npx prisma db push
```

### 5. Готово! 🎉

Теперь при каждом `git push origin main` будет автоматический деплой!

## Что происходит автоматически:

- ✅ Push в `main` → Автоматический деплой
- ✅ Pull Request → Preview деплой
- ✅ `prisma generate` → Выполняется автоматически
- ✅ Build → Next.js production build
- ✅ Deploy → Распространяется по CDN глобально

## Полезные команды:

```bash
# Посмотреть логи деплоя
vercel logs

# Посмотреть текущий деплой
vercel ls

# Открыть проект в браузере
vercel open
```

## Проблемы?

Смотрите подробную инструкцию: [VERCEL_DEPLOY.md](./VERCEL_DEPLOY.md)

---

**Разработано с помощью Claude Sonnet 4.5** 🤖
