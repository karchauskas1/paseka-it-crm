# 🚀 Запуск PASEKA IT CRM - Пошаговая инструкция

## ⚡️ Быстрый старт (5 минут)

### 1. Убедитесь, что установлено:
- Node.js 18+ (проверьте: `node --version`)
- PostgreSQL 14+ (проверьте: `psql --version`)

Если нет PostgreSQL:
```bash
# macOS
brew install postgresql@16
brew services start postgresql@16

# Ubuntu/Debian
sudo apt-get install postgresql
sudo systemctl start postgresql

# Windows - скачайте с https://www.postgresql.org/download/
```

### 2. Создайте базу данных

```bash
# Войдите в PostgreSQL
psql postgres

# В psql выполните:
CREATE DATABASE paseka_crm;

# Проверьте, что база создана:
\l

# Выйдите:
\q
```

### 3. Установите зависимости

```bash
npm install
```

### 4. Настройте базу данных с Prisma

```bash
# Применить схему к БД
npm run db:push

# Сгенерировать Prisma Client
npm run db:generate
```

Если появляются ошибки, попробуйте:
```bash
npx prisma generate
npx prisma db push
```

### 5. Запустите проект

```bash
npm run dev
```

Откройте в браузере: **http://localhost:3000**

### 6. Создайте первого пользователя

1. Нажмите "Зарегистрироваться"
2. Заполните:
   - Имя: `Данила` (или ваше имя)
   - Email: `danila@paseka.it`
   - Пароль: `paseka123`
3. Нажмите "Зарегистрироваться"

**Готово!** Вы в системе. 🎉

---

## 📝 Следующие шаги

### Создайте первого клиента

1. Перейдите в "Клиенты" (верхнее меню)
2. Нажмите "Создать клиента"
3. Заполните:
   - Имя: `Иван Петров`
   - Компания: `ООО "Пример"`
   - Email: `ivan@example.com`
   - Источник: Выберите "Рекомендация"
4. Сохраните

### Создайте первый проект

1. Перейдите в "Проекты"
2. Нажмите "Создать проект"
3. Заполните:
   - Клиент: Выберите созданного клиента
   - Название: `CRM для интернет-магазина`
   - Тип: `Деньги`
   - Статус: `Квалификация`
   - Описание: `Разработка CRM системы для управления заказами`
   - Бюджет: `500000`
4. Сохраните

### Добавьте боль клиента и получите AI-анализ

1. Откройте созданный проект
2. Перейдите на вкладку "Боль и контекст"
3. Заполните поле "Описание боли":
   ```
   Клиент теряет заказы из-за отсутствия централизованной системы учёта.
   Менеджеры используют Excel и Telegram, данные дублируются и теряются.
   Процесс обработки заказа занимает 2-3 дня вместо нескольких часов.
   ```
4. Нажмите "Анализ AI" (если настроили OpenRouter API)
5. AI предложит варианты решения

### Создайте задачу

1. В проекте перейдите на вкладку "Задачи"
2. Нажмите "Создать задачу"
3. Заполните:
   - Название: `Провести брифинг с клиентом`
   - Описание: `Узнать детали бизнес-процессов`
   - Статус: `TODO`
   - Приоритет: `Высокий`
   - Срок: Выберите дату через 3 дня
4. Сохраните

---

## 🤖 Настройка AI (опционально)

Если хотите использовать AI-функции:

### 1. Получите API ключ OpenRouter

1. Зарегистрируйтесь на https://openrouter.ai/
2. Пополните баланс ($5 хватит на ~1000 запросов)
3. Перейдите в Settings → API Keys
4. Создайте новый ключ
5. Скопируйте ключ (начинается с `sk-or-v1-...`)

### 2. Добавьте ключ в .env

Откройте файл `.env` в корне проекта и добавьте:
```env
OPENROUTER_API_KEY="sk-or-v1-ваш-ключ-здесь"
```

### 3. Перезапустите сервер

```bash
# Остановите сервер (Ctrl+C)
# Запустите снова:
npm run dev
```

Теперь AI будет:
- Анализировать боль клиента
- Предлагать архитектуру решения
- Помогать с заполнением полей

---

## 📱 Настройка Telegram уведомлений (опционально)

Если хотите получать уведомления в Telegram:

### 1. Создайте бота

1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте команду: `/newbot`
4. Дайте боту имя: `PASEKA CRM Bot` (или любое другое)
5. Дайте username: `paseka_crm_bot` (или любой свободный)
6. Скопируйте токен (выглядит как `1234567890:ABCdef...`)

### 2. Добавьте токен в .env

```env
TELEGRAM_BOT_TOKEN="1234567890:ABCdef-ваш-токен-здесь"
```

### 3. Перезапустите сервер

```bash
npm run dev
```

### 4. Привяжите свой Telegram

1. Найдите бота в Telegram по username
2. Отправьте `/start`
3. Бот пришлёт ваш Telegram ID (например: `123456789`)
4. Скопируйте этот ID
5. В CRM перейдите в настройки профиля
6. Вставьте ID в поле "Telegram ID"
7. Сохраните

Теперь вы будете получать уведомления!

---

## 🔧 Устранение проблем

### Ошибка: "Cannot connect to database"

**Решение:**
```bash
# Проверьте, что PostgreSQL запущен:
pg_isready

# Если не запущен, запустите:
# macOS:
brew services start postgresql@16

# Linux:
sudo systemctl start postgresql

# Проверьте, что база данных существует:
psql -l | grep paseka_crm
```

### Ошибка: "Prisma Client is not generated"

**Решение:**
```bash
npx prisma generate
```

### Ошибка: "Port 3000 is already in use"

**Решение - используйте другой порт:**
```bash
PORT=3001 npm run dev
```

Затем откройте http://localhost:3001

### Ошибка при npm install

**Решение:**
```bash
# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Установите заново
npm install
```

### Prisma migration ошибка

**Решение - сброс БД (ВНИМАНИЕ: удалит все данные):**
```bash
npx prisma db push --force-reset
```

### TypeScript ошибки

**Решение:**
```bash
rm -rf .next
npm run dev
```

---

## 📚 Дополнительная информация

- **Полная документация**: [README.md](README.md)
- **Быстрый старт**: [QUICKSTART.md](QUICKSTART.md)
- **Prisma Studio** (просмотр БД): `npm run db:studio`

---

## ✅ Чек-лист готовности к работе

- [ ] PostgreSQL установлен и запущен
- [ ] База данных `paseka_crm` создана
- [ ] `npm install` выполнен успешно
- [ ] `npm run db:push` выполнен успешно
- [ ] Сервер запущен (`npm run dev`)
- [ ] Открывается http://localhost:3000
- [ ] Зарегистрирован первый пользователь
- [ ] Создан первый клиент
- [ ] Создан первый проект

**Опционально:**
- [ ] Настроен OpenRouter API (AI функции)
- [ ] Настроен Telegram Bot (уведомления)

---

## 🎯 Что дальше?

1. **Пригласите команду** - добавьте Анатолия и других участников
2. **Создайте реальные проекты** - начните работать с настоящими клиентами
3. **Настройте AI** - получите помощь в анализе и архитектуре
4. **Подключите Telegram** - будьте в курсе всех изменений
5. **Кастомизируйте** - добавьте свои поля и процессы

---

**Нужна помощь?**
- Проверьте логи в консоли
- Прочитайте [README.md](README.md)
- Откройте Prisma Studio: `npm run db:studio`

**Готовы к работе?** Начните с создания первого клиента! 🚀
